name: A-Line Daily Scrape

on:
  schedule:
    # 06:00 UTC = 07:00 CET / 14:00 SGT
    - cron: '0 6 * * *'
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Just scrape, skip Sheets write'
        required: false
        default: 'false'

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt && pip install python-jobspy gspread google-auth

      - name: Write Google credentials
        run: echo '${{ secrets.GOOGLE_CREDENTIALS }}' > credentials.json

      - name: Run scraper
        env:
          JSEARCH_API_KEY: ${{ secrets.JSEARCH_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          APOLLO_API_KEY: ${{ secrets.APOLLO_API_KEY }}
          JOBSPY_PROXY: ${{ secrets.JOBSPY_PROXY }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          ALERT_EMAIL: ${{ secrets.ALERT_EMAIL }}
        run: python quick_run.py

      - name: Upload CSV backup
        uses: actions/upload-artifact@v4
        with:
          name: leads-${{ github.run_number }}
          path: leads_*.csv
          retention-days: 30
