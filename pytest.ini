[pytest]
testpaths = tests
markers =
    unit: Pure function tests (no mocking, no network)
    integration: Business logic flow tests (mocked at function level)
    e2e: End-to-end tests (mocked at HTTP level)
pythonpath = .
addopts = -v --tb=short --strict-markers
